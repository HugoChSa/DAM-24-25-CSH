package ud1.trabajosclase.csh20241004;

        /**EP0123_ConversionMasa. Escribe un programa que convierta unidades de masa entre kilogramos y libras o viceversa. El programa solicitará al usuario la cantidad de masa y la unidad
 * de medida origen (kilogramos o libras). 
 * 1 kilogramo = 1000 gramos 
 * 1 libra = 453.592 gramos
 * El programa deberá manejar las conversiones automáticamente y mostrar el resultado al usuario. Utiliza el operador ternario para evaluar la unidad de medida introducida por el
 * usuario. El programa mostrará por pantalla el resultado con la unidad de medida adecuada.
 */
/*
import java.util.Scanner;
public class EP0123 {
    public static void main(String[] args) {
        double masaOrigen, masaDestino;
        
        final double GRAMOS_LIBRA = 453.592;
        final double KILOS_LIBRAS = 1000 / GRAMOS_LIBRA;
        final double LIBRAS_KILOS = GRAMOS_LIBRA / 1000;

        Scanner sc = new Scanner(System.in);
        System.out.println("Introduce la masa de origen: ");
        masaOrigen = sc.nextDouble(); */
        
        /*byte unidadDeMedida;
        System.out.println("Introduce la medida de origen:  (1 = Kilos, 2 = Libras): ");
        unidadDeMedida = sc.nextByte();*/
    
/*      char unidadDeMedida;
        System.out.println("Introduce la medida de origen:  (K = Kilos, L = Libras): ");
        unidadDeMedida = sc.nextLine().charAt(0);

        masaDestino = unidadDeMedida == 'K' ? masaOrigen * KILOS_LIBRAS : masaOrigen * LIBRAS_KILOS;

        String unidadmedidaDestino = unidadDeMedida == 'K' ? "libras" : "kilos";

        sc.close();
        
        System.out.printf("La masa en la medida del destino es %.3f %s %n", masaDestino, unidadDeMedida ==1 ? "libras" : "kilos");

    }
    
}
*/