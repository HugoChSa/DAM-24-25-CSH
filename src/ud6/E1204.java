package ud6;

import java.util.Scanner;

public class E1204<T> {

/*
    public static void main(String[] args) {
    int num;
    Scanner entrada = new Scanner(System.in);
    System.out.println("Introduzca numeros: ");
    num = entrada.nextInt();


    }
    private T num;
    public Pares {
        if (T % 2) {}
        System.out.println(numPares);
    }

    /*
    private T objeto; // se inicializa a null: contenedor vacío

    public Contenedor() {
    }

    void guardar(T nuevo) {
        objeto = nuevo;
    }

    T extraer() {
        T res = objeto;
        objeto = null; // el contenedor vuelve a estar vacio
        return res;

    }
    class introduccionNumeros {
        T[] objetos;
        <T> void insertarAlPrincipio(T nuevo) {
            objetos = Arrays.copyOf(objetos, objetos.length + 1);
            for (int i = objetos.length - 1; i > 0; i--) {
                objetos[i] = objetos[i - 1];
            }
            objetos[0] = (T) nuevo;
    }

}*/
}
